#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export CODEX_MEMO_COMMAND_PATH="${BASH_SOURCE[0]:-$0}"
exec python3 "$ROOT_DIR/scripts/codex_memo.py" "$@"
